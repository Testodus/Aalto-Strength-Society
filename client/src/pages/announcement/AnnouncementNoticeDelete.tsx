import React from 'react';
import BasicFrame from '../components/BasicFrame';

/**
 * This will be the error page that is displayed when there is error in routing (not connected yet)
 */
const AnnouncementNoticeDelete = () => {
  return <BasicFrame topic={'delete'}></BasicFrame>;
};

export default AnnouncementNoticeDelete;
