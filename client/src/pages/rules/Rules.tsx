import React from 'react';
import BasicFrame from '../components/BasicFrame';

/**
 * Rules page, uses atm the basic-frame
 * @returns Rules component
 */
const Rules = () => {
  return <BasicFrame topic={'rules'}></BasicFrame>;
};

export default Rules;
